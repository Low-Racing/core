[build]
  command = "npm ci && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18.x"
  NPM_FLAGS = "--no-audit --no-optional"

[[plugins]]
  package = "@netlify/plugin-nextjs"